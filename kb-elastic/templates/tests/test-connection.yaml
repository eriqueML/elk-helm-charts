apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kb-elastic.fullname" . }}-test-connection"
  labels:
{{ include "kb-elastic.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kb-elastic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
